package de.master.lobby.core.listener.invs;

import de.master.lobby.core.LobbyPlugin;
import org.bukkit.entity.Player;

public class SettingsBooleans {

    /*
    public static boolean[] calcBooleans(Player p) {
        boolean b1 = LobbyPlugin.getInstance().getMySQL().getSqlStats().getPlayerVisibility(p.getUniqueId().toString());
        boolean b2 = LobbyPlugin.getInstance().getMySQL().getSqlStats().getGUIAnimations(p.getUniqueId().toString());
        boolean b3 = LobbyPlugin.getInstance().getMySQL().getSqlStats().getFightMode(p.getUniqueId().toString());
        boolean b4 = LobbyPlugin.getInstance().getMySQL().getSqlStats().getSounds(p.getUniqueId().toString());

        boolean[] booleans = new boolean[4];
        booleans[0] = b1;
        booleans[1] = b2;
        booleans[2] = b3;
        booleans[3] = b4;

        return booleans;
    }
     */

}
